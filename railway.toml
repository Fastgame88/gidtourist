[build]
builder = "DOCKERFILE"
watchPatterns = ["/app/**", "/public/**", "/package.json", "/package-lock.json", "/next.config.ts", "/Dockerfile", "/railway.toml"]

[deploy]
healthcheckPath = "/api/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
