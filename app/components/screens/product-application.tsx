// Compatibility shim for repositories that still contain the old misplaced
// product-application file under app/components/screens.
// The real implementation lives one level up.
export { default } from "../product-application";
